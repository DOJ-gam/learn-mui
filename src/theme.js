import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#B90000",
      light: "#B93730",
    },
    secondary: {
      main: "#F99999",
    },
    dojColor: {
      main: "#440888",
      light: "#ccc",
    },
  },
});
